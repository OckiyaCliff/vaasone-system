/* ──────────────────────────────────────────────────────────
   Vaasone — SIS Adapter Interface
   All adapters implement this contract to normalize
   heterogeneous data sources into the canonical model.
   ────────────────────────────────────────────────────────── */

export type { SISAdapter, CanonicalCredential, ConnectionConfig, ConnectionTestResult } from '@/lib/types'
